# **پروژه نهایی درس برنامه نویسی دستگاه های سیار اپلیکیشن استریم موسیقی و شبکه اجتماعی** 

استاد درس : طارمی راد  معصومه  دکتر 

دانشکده مهندسی کامپیوتر ترم بهار1405 

## فهرست مطالب 

1 ...................................................................................................................... . مقدمه1 1 .......................................... )Clean Code & System Design2. قوانین پایه( ای و استانداردهای توسعه 2 ........................................................... )Architecture & Tech Stack ( . معماری و زیرساخت فنی3 2 ............................................................ )Backend & Mock Data4. سمت سرور و داده( های اولیه 3 ........................................................... )UI/UX in Compose5. جزئیات رابط کاربری و انیمیشن ( ها 3 .............................................................................................. )Home( تب اول: خانه 1.5 4 .......................................................................................... )Search( تب دوم: جستجو 2.5 4 ................................................................................... )Downloads( تب سوم: دانلودها 3.5 4 ................................................................................ )Playlistsلیست( ها تب چهارم: پلی4.5 4 ................................................................................................ تب پنجم: پروفایل من 5.5 5 ................................................................................. )Media Playback6. مدیا پلیر حرفه( ای 6 ........................................................................................ )Business Logic( . منطق برنامه7 6 ..................................................................... )Real-time Chat( . شبکه اجتماعی و سیستم چت8 7 ................................................................................................... 9. مدیریت داده های محلی 7 ....................................................................... 10. صفحات فرعی، خالقیت و استقالل توسعه دهنده 8 ..................................................................................................... . خروجی مورد انتظار11 

## 1. مقدمه 

) همراه با امکانات یک شبکه اجتماعیMelodify یا Spotifyپ روژه نهایی شما، توسعه یک پلتفرم استریم موسیقی (مشابه توانید هر نام خالقانه.ای که دوست دارید برای اپلیکیشن خود انتخاب کنید کوچک است. شما می 

اند اختصاصی تا پیاده سازی مفاهیم اندرویددر این پروژه، شما تمام چرخه توسعه نرم افزار از طراحی دیتابیس و بک و پخش مدیا در پس زمینه را را انجام داده )Jetpack Compose, Clean Architecture, UDF, Coroutines/Flow( )، پیاده سازیClean Code ( هدف ما در این پروژه، ارزیابی توانایی شما در نوشتن کد تمیزپیاده.سازی خواهید کرد .) است State Managementهای پیچیده و مدیریت بهینه وضعیت( هاانیمیشن 

) Clean Code & System Design2. قوانین پایه ای( و استانداردهای توسعه 

یش از پرداختن به امکانات، رعایت استانداردهای زیر در تمام بخش:های کد الزامی و دارای نمره مستقیم است پ 

ها و فاصله هاها، سایز فونت برای رنگ Hardcode از مقادیر ): استفاده کردن Design System دیزاین( سیستم `o` ،Typography  سفارشی شامل MaterialTheme  ) اکیداً ممنوع است. شما باید یکPadding/Margin( .تعریف کنید Shapes و Colors کلین کد و منابع متنی: هیچ متنی نباید به صورت مستقیم در کد نوشته شود. تمامی متون باید از طریق فایل `o` .فراخوانی شوند strings.xml 

): اپلیکیشن باید به صورت کامل دو زبانه (فارسی و انگلیسی) باشد. چیدمانLocalization پشتیبانی( از دو زبان `o` (چپ چین برایLTR (راست چین برای فارسی) وRTL ) باید به صورت خودکار با تغییر زبان بینLayout( .انگلیسی) تغییر کند 

یا ): اپلیکیشن باید دارای هر دو تم باشد و با تغییر تنظیمات سیستم و Dark/Light Mode تم ( تاریک و روشن `o` از داخل خود برنامه تغییر کند. پالت رنگی باید برای هر دو حالت بهینه سازی شده باشد تا خوانایی متون حفظ .شود Adaptive Iconاختصاصی: استفاده از آیکون پیش فرض اندروید پذیرفته نیست. اپلیکیشن باید دارای آیکون `o` اختصاصی باشد . 

1 

) Architecture & Tech Stack( . معماری و زیرساخت فنی3 

سازی شود . ) پیادهData, Domain, UI و الیه( بندی مناسبClean Architectureپ روژه باید با رعایت دقیق اصول 

تمامی. (Unidirectional Data Flow) UDF با رویکرد MVVM یا MVI : استفاده ازUI الگوی طراحی `o` به کامپوز StateFlow تجمیع شوند و از طریق UiState به نام Data Classوضعیت های هر صفحه باید در یک .پاس داده شوند 

ارسال شوند. رویدادهای یک بار ViewModel به Event یا Intent مدیریت رویدادها: تعامالت کاربر باید در قالب `o` .مدیریت شوند Channel یا SharedFlow خطای شبکه) باید با Snackbar مصرف (مثل نمایش ،هاRepository( الزامی استی برنامه ها برای مدیریت وابستگیKoin یا Hilt ): استفاده ازDI تزریق( وابستگی `o` .)ها، دیتابیس و کالینت شبکه باید تزریق شوندUseCase ): برای تمامی لیست های طوالنی (جستجو، لیست آهنگ هایPaging 3مدیریت جریان داده در لیست( ها `o` الزامی  است.  لود  کردن  کل  داده ها  در  یکPaging 3پلی  لیست  و  تاریخچه  چت)  استفاده  از  کتابخانه .پذیرفته نیست LazyColumn 

.اجرا شود Main Thread: هیچ عملیات شبکه  ای یا دیتابیسی نباید رویThread Safety  و I/Oمدیری  ت `o` و کروتین.ها ضروری استDispatchers.IO استفاده صحیح از 

) Backend & Mock Data4. سمت سرور و داده( های اولیه 

یا کدهای سفارشی Supabase ،Firebase مثل BaaSتوانید از سرویس هایاند بر عهده خود شماست. مییاده سازی بکپ .استفاده کنید Ktor/Node.js با 

های بی کالم، قطعات آهنگ واقعی باشد (می توانید از آهنگ محتوای الزامی: دیتابیس شما باید شامل حداقل50 `o` کوتاه و .)یا هر منبعی استفاده کنید :زیر باشد :متادیتا هر آهنگ باید حداقل دارای فیلدهای `o` 

id • title • artist_name • cover_image_url • audio_url • 

2 

## UI/UX in Compose5. جزئیات رابط کاربری و انیمیشن( ها 

، افکتFluid های مقیاس پذیری های بصری باشد. استفاده از انیمیشن هایکاریابط کاربری باید مدرن، روان و پر از ریزهر های نرم در کل برنامه انتظار می.رود ویژگی های عمومی مورد انتظارTransition) در هنگام کلیک روی دکمه ها وScale( :به شرح زیر هستند  در صفحات 

در تمامی صفحات اصلی، سمت راست لوگو و نام اپلیکیشن و سمت چپ عکس پروفایل کاربر، آیکون :Top Bar `o` ها) و چرخ .دنده (تنظیمات) قرار داردزنگوله (اعالن ها و کارت ها باید انیمیشنهای لیستاند، تمامی بخشها از سرور لود نشده :تا زمانی که دادهShimmer Effect `o` .) داشته باشندShimmer( اسکلتون لودینگ ): جابجایی بین مینی پلیر در پایین صفحه و باز شدن کاملShared Element Transitions انتقال( صفحات `o` پیاده سازی شود تا کاور آهنگ به نرمی بزرگ شدهShared Transitionصفحه پلیر اصلی، باید با انیمیشن های 

.و در مرکز صفحه قرار گیرد ) Home( تب اول: خانه 1.5 

های زیر تشکیل شده است: این صفحه ویترین برنامه شماست و از بخش 

): در ب لترین بخش صفحه، یک اسالیدر افقی متحرک قرار دهید که Carousel( پیشنهادات روز  اسالیدر `o` های ترند را با تصاویر بزرگ نمایش می .دهدآلبوم های جدید، پیشنهادات ویژه روز یا آهنگ شامل موارد زیر است: ): چهار دکمه کهQuick Actions دسترسی( سریع `o` آهنگ های الیک شده • • اخیراً شنیده شده لیست های منپلی • هنرمندان برتر • و های  جهانی»لیست«پلی  ،ها»«جدیدترین  ،ترین ها»«محبوبهایی  برای  ):  ردیفLazyRowاسالیدر(  ها `o` های محلی» .لیست«پلی 

3 

) Search( تب دوم: جستجو 2.5 

): با هر بار تایپ کردن، نباید ریکوئست به سرور ارسال شود. شما باید باDebounce جستجوی( زنده و هوشمند `o` .، تاخیر مناسبی ایجاد کنیدFlow در debounce استفاده از عملگر ... و.)هایی برای فیلتر کردن نتایج (آهنگ، خواننده های فیلتر: دکمهپ چی `o` .) با امکان حذف Roomت اریخچه: نمایش تاریخچه جستجوهای قبلی (ذخیره شده در `o` 

) Downloads ( تب سوم: دانلودها 3.5 

های دانلود شده برای گوش دادن در حالت آفالین است که قابلیت مرتب سازی و حذف با کشیدن شامل لی ستی از آهنگ ) را نیز داراست. Swipe to Dismiss( 

) Playlistsلیست( ها تب چهارم: پلی4.5 

برای نمایش دو ستونه کارت .های رنگیLazyVerticalGridا ستفاده از `o` های کاربر». لیست و «پلی موسیقی داخلی» « ، موسیقی جهان»خش ب بندی به« `o` 

تب پنجم: پروفایل من 5.5 

ت .صویر آواتار کاربر با قابلیت تغییر :آوارتار `o` .) وضعیت اشتراک ویژهBadge( ): نمایش نشانگر طالییPremium Statusو( ضعیت اشتراک `o` کاربر Premiumبر روی این دکمه، یک فرآیند نمایشی طی شده و متغیر با کلیککمه تمدید/خرید اشتراک: د `o` 

می.شودtrue تنظیمات: د .)سترسی به تنظیمات برنامه (تغییر زبان و تم `o` 

4 

## )Media Playback6. مدیا پلیر حرفه( ای 

در بر می .گیرد یاده سازی این بخش بیشترین بار فنی پروژه راپ 

پس زمینه در سرویس یک صورت به باید موسیقی پخش   عملیات  :MediaSession  و ExoPlayer `o` .) انجام شود. اگر کاربر از برنامه خارج شد، موزیک نباید قطع شودBackground Service( : پلیر شما باید هوشمند باشد. با دریافت تماس ورودی یا پخش صدای سیستم (مثل وویسAudio Focus `o` .) و پس از اتمام، مجدداً پخش شودDuck ( تلگرام)، آهنگ باید موقتاً متوقف شده یا صدای آن کم شود ): وقتی کاربر یک آهنگ را استریم می کند، فایل آن بایدExoPlayer CacheDataSource( کش هوشمند `o` یا عقب/جلو کردن، دیتای اینترنت مجدداً مصرف Replay ذخیره شود تا در صورت ExoPlayer همزمان در کش .نشود Crossfade `o` های پایانی و ترکیب آن با ثانیه .های ابتدایی آهنگ بعدی: محو شدن صدای آهنگ در ثانیه اسکرین،  همراه  با  دکمه هایوتیفیکیشن  کنترل:  نمایش  وضعیت  پخش  در  نوار  اعالن  سیستم  و  الکن `o` .Play/Pause/Next 

شناور است و آهنگ Bottom Navigation ): یک نوار کوچک که همیشه در ب لی Mini Playerم( ینی پلیر `o` در حال پخش را نشان می .دهد :)Now Playing Screenر( ابط کاربری صفحه پلیر `o` ) قرار دارد که در زمان پخشCDک( اور متحرک: در وسط صفحه، کاور آهنگ به شکل یک دیسک • چرخد و با توقف آهنگ، متوقف می.شودآهنگ می ): رنگ پس زمینه صفحه پلیر نباید ثابت باشد. رنگ غالب کاور آهنگ بایدPalette APIر نگ( بندی پویا • استخراج شده و به صورت گرادیانت نرم روی پس.زمینه اعمال شود ) انیمیشنی در صفحهWaveform( ): یک اکوالیزر یا موج صوتیCanvas (رسم با Audio Visualizer • ممنوع است و باید GIF یا Lottie پلیر طراحی کنید که در حین پخش آهنگ حرکت کند. (استفاده از .)رسم شود Compose در Canvas حتماً با 

دقیقه ): قابلیتی پیاده ًسازی کنید که کاربر بتواند با تنظیم زمان (مثال15Sleep Timerت( ایمر خواب • .صادر کند Coroutines دیگر)، دستور توقف خودکار پخش را با استفاده از . ExoPlayer از طریق کنترلرهای 2x یا 1.5xک نترل سرعت پخش: امکان تغییر سرعت پخش آهنگ به • 

5 

) Business Logic( . منطق برنامه7 

): کاربران عادی می توانند تمام آهنگ ها را به صورت کامل وPremium Logicت( فاوت کاربر عادی و ویژه `o` نامحدود گوش دهند و هیچ توقفی در کار نیست. تفاوت تنها در قابلیت دانلود آفالین است. دکمه دانلود فقط فعال است و اگر کاربر عادی روی آن کلیک کند، باید پیامی مبنی بر نیاز به ارتقاء Premium برای کاربران .حساب کاربری ببیند 

: زمانی که کاربر ویژه یک آهنگ را دانلود می کند، این فرآیند باید بهWorkManagerم  دیریت دانلودها با `o` سپرده شود تا در پس .زمینه انجام گیردWorkManager آهنگی را زد و فایل آفالین آن دانلود Play باید چک کند اگر کاربر دکمه Repositoryپ خش هوشمند: الیه `o` .شده بود، به جای مصرف ترافیک اینترنت، موزیک مستقیماً از روی فایل لوکال پخش شود 

) Real-time Chat . شبکه اجتماعی و( سیستم چت8 

ها و جریان:های داده است ین بخش معیار اصلی ارزیابی توانمندی شما در مدیریت سوکتا 

لیست:های  پابلیک  کاربران  می توانند  پروفایل  سایر  کاربران )  و  پلیFollow Usersد (  نبال  کردن  دوستان `o` لیست های عمومی دوستان خودتوانند پلیکاربران می همچنینها را دنبال کنند. جستجو کرده و آن (دوستان) را .را دیده و پخش کنند 

لیست پیام های جدید باید به و باشد Real-time ً) باید کامالDM( : بخش دایرکت مسیجWebSocket اتصال `o` .به هیچ عنوان پذیرفته نیست Pollingصورت زنده آپدیت شود . استفاده از ،)(ساعتی ارسالی باید دارای وضعیت های«در حال ارسال » ها): پیامRead/Delivered Receipts( رسید پیام `o` .(دو تیک) باشند (تک تیک) و«خوانده شده» «ارسال شده » زمانی که کاربر مقابل در حال» is typing... ): نمایش عبارت«Typing Indicator( وضعیت در حال نوشتن `o` .تایپ است 

پیام حاوی، آهنگ را به دایرکت دوست ش.ارسال کند Shareکاربر می تواند از طریق دکمهاشتراک :گذاری آهنگ `o` .) داشته باشد که با کلیک روی آن مستقیماً در پلیر پخش شودMini Card ( سفارشی UI آهنگ باید یک ،ذخیره شوند تا در صورت قطعی اینترنت Roomهای دریافت شده باید در دیتابیسها: پیاممدیریت آفالین چت `o` .کاربر بتواند تاریخچه را بخواند 

6 

های محلی 9. مدیریت داده 

Room Database `o` های الیک شده، آدرس فایل های دانلود شده و تاریخچه : تاریخچه جستجو، لیست آهنگ پیام.های آفالین همگی باید در دیتابیس گوشی ذخیره شوند وضعیت کاربرمود، اندازه فونت، زبان انتخابی و : تنظیمات برنامه مانند حالت دارکDataStore Preferences `o` (عادی/ویژه) باید با دیتا استور مدیریت شوند. 

دهنده 10. صفحات فرعی، خالقیت و استقالل توسعه 

) حیاتی به صورت دقیقFlowsر مستندات این پروژه، ساختار صفحات کلیدی، معماری سیستم و جریان( های کارید شود. نویس دیکته نمیها و صفحات فرعی مو به مو به برنامهشرح داده شد. با این حال همیشه تمام جزئیات میکرواینتراکشن 

) وUXرود سایر صفحات فرعی و بخش( های تکمیلی اپلیکیشن را با تکیه بر درک خود از تجربه کاربریاز شما انتظار می ) برنامه، طراحی و پیاده .سازی کنید برخی از این صفحات و بخش هاDesign System( با حفظ یکپارچگی دیزاین سیستم عبارتند از : 

ص فحه آهنگ `o` های الیک شده / اخیراً شنیده شده: انتظار می رود این صفحات صرفاً یک لیست ساده نباشند بلکه ) و قابلیت حذف سریع آیتم ها از لیست (مثالً باShuffle/Play All دارای هدر جذاب، دکمه «پخش هم»ه ( .) باشندSwipe to Dismiss انیمیشن 

ل `o` یست هنرمندان و کاربران دنبال شده: یک رابط کاربری تمیز (مثالً گرید یا لیست) برای مدیریت کسانی که کاربر .Unfollowآن ها را فالو کرده است، همراه با دکمه ،)RTL/LTR): صفحه  ای استاندارد برای تغییر زبان اپلیکیشن (تغییر داینامیکSettingsص( فحه تنظیمات `o` .تغییر تم (تاریک/روشن/سیستم) و خروج از حساب کاربری 

): طراحی و قرار دادن تصاویر یا انیمیشن های مناسب برای زمانی که یکEmpty Statesالت ح( های خالی `o` ای ندارد یا اینترنت قطع است.لیست خالی است، جستجو نتیجهپلی 

7 

ظار روجی مورد انت11. خ 

پروژه )نیست source codeی به آپلو دیشن خروجی (نیازایل اپلیک ف `o` رد ا پلیکیشنلکز نحوه ی عموی کوتاه ا یک ئد وی `o` 

8 

